<?xml version="1.0" encoding="utf-8"?>
<!-- RecyclerView item for one Memory Match card (Lecture 11) -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/memory_card_height"
    android:orientation="vertical"
    android:padding="@dimen/card_margin">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@color/card_back_color"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center">

            <!-- Shows the card value when face-up; hidden when face-down -->
            <TextView
                android:id="@+id/tvCardValue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="7"
                android:textSize="28sp"
                android:textColor="@color/purple_primary"
                android:textStyle="bold"
                android:visibility="invisible" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

</LinearLayout>
